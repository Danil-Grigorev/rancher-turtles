---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clusterregistrationtokens.management.cattle.io
spec:
  group: management.cattle.io
  names:
    kind: ClusterRegistrationToken
    listKind: ClusterRegistrationTokenList
    plural: clusterregistrationtokens
    singular: clusterregistrationtoken
  scope: Namespaced
  versions:
  - name: v3
    schema:
      openAPIV3Schema:
        description: ClusterRegistrationToken is the struct representing a Rancher
          ClusterRegistrationToken.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRegistrationTokenSpec is the struct representing the
              spec of a Rancher ClusterRegistrationToken.
            properties:
              clusterName:
                type: string
            required:
            - clusterName
            type: object
          status:
            description: ClusterRegistrationTokenStatus is the struct representing
              the status of a Rancher ClusterRegistrationToken.
            properties:
              manifestUrl:
                type: string
            required:
            - manifestUrl
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
